M = (E; Q; q0; F; T), AFN L

M = (E; Q; q0; F; T) AFN

M = (E; Q; q0; F; S) AFM



E alfabeto

E* alfabeto concatenado eterno

Q conjunto de estados 

q0 estado inicial

F conjunto aceptacion

S funcion transicion uno por uno, (por cada elemento del alfabeto lo mueve a otro)

T funcion transicion uno pro varios, (por cada elemento del alfabeto lo mueve a uno o mas esados)

T AFN L pero con Lamba 